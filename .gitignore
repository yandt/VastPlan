# OS
.DS_Store
Thumbs.db

# 编辑器 / 工具本地配置
.claude/settings.local.json
.vscode/*.local
*.swp

# Obsidian 工作区本地状态（保留 vault 配置，忽略个人工作区，任意层级）
**/workspace.json
**/workspace-mobile.json

# 日志 / 临时
*.log
tmp/
.tmp/

# 构建产物
bin/
.gstack/
__pycache__/
*.py[cod]

# 本地 Agent/graphify 运行状态。仓库只保留可移植、可查询的图数据；解释器路径、
# 缓存、诊断和可再生成展示产物不得进入版本库，避免泄露本机绝对路径。
.codex/
graphify-out/
