{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.cdsoft.com.cn/vastplan/database/v1/vastplan.database-runtime.schema.json",
  "title": "VastPlan Database Runtime v1",
  "oneOf": [
    { "$ref": "#/$defs/providerListRequest" },
    { "$ref": "#/$defs/metricsRequest" },
    { "$ref": "#/$defs/probeRequest" },
    { "$ref": "#/$defs/activateRequest" },
    { "$ref": "#/$defs/retireRequest" },
    { "$ref": "#/$defs/queryRequest" },
    { "$ref": "#/$defs/executeRequest" },
    { "$ref": "#/$defs/beginRequest" },
    { "$ref": "#/$defs/endTransactionRequest" }
  ],
  "$defs": {
    "identifier": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$",
      "maxLength": 160
    },
    "providerId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*(?:[._-][a-z0-9]+)*$",
      "maxLength": 80
    },
    "providerCapabilities": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "query": { "type": "boolean" },
        "execute": { "type": "boolean" },
        "transactions": { "type": "boolean" },
        "readOnlyTransactions": { "type": "boolean" },
        "savepoints": { "type": "boolean" },
        "streaming": { "type": "boolean" },
        "namedParameters": { "type": "boolean" }
      },
      "required": ["query", "execute", "transactions"]
    },
    "providerDescriptor": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": { "$ref": "#/$defs/providerId" },
        "version": { "$ref": "https://schemas.cdsoft.com.cn/vastplan/common/v1/identifiers.schema.json#/$defs/semver" },
        "displayName": { "type": "string", "minLength": 1, "maxLength": 160 },
        "configurationSchema": { "type": "object" },
        "capabilities": { "$ref": "#/$defs/providerCapabilities" }
      },
      "required": ["id", "version", "displayName", "configurationSchema", "capabilities"]
    },
    "connectionRef": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "resourceId": { "$ref": "#/$defs/identifier" },
        "revision": { "type": "integer", "minimum": 1 }
      },
      "required": ["resourceId", "revision"]
    },
    "poolPolicy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "minIdle": { "type": "integer", "minimum": 0, "maximum": 10000 },
        "maxIdle": { "type": "integer", "minimum": 0, "maximum": 10000 },
        "maxOpen": { "type": "integer", "minimum": 1, "maximum": 10000 },
        "maxLifetimeMs": { "type": "integer", "minimum": 1000, "maximum": 86400000 },
        "maxIdleTimeMs": { "type": "integer", "minimum": 1000, "maximum": 86400000 },
        "acquireTimeoutMs": { "type": "integer", "minimum": 100, "maximum": 300000 },
        "idlePoolTtlMs": { "type": "integer", "minimum": 1000, "maximum": 86400000 }
      },
      "required": ["maxIdle", "maxOpen", "maxLifetimeMs", "maxIdleTimeMs", "acquireTimeoutMs", "idlePoolTtlMs"]
    },
    "connectionSpec": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "ref": { "$ref": "#/$defs/connectionRef" },
        "providerId": { "$ref": "#/$defs/providerId" },
        "endpoint": { "type": "string", "minLength": 1, "maxLength": 2048 },
        "database": { "type": "string", "maxLength": 256 },
        "options": { "type": "object", "maxProperties": 128 },
        "credentials": { "$ref": "https://schemas.cdsoft.com.cn/vastplan/common/v1/identifiers.schema.json#/$defs/managedCredentialRef" },
        "pool": { "$ref": "#/$defs/poolPolicy" }
      },
      "required": ["ref", "providerId", "endpoint", "options", "credentials", "pool"]
    },
    "value": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "type": { "type": "string", "enum": ["null", "string", "int64", "decimal", "bool", "bytes", "timestamp", "json"] },
        "value": {}
      },
      "required": ["type"]
    },
    "statement": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "sql": { "type": "string", "minLength": 1, "maxLength": 65536 },
        "parameters": { "type": "array", "maxItems": 1000, "items": { "$ref": "#/$defs/value" } }
      },
      "required": ["sql", "parameters"]
    },
    "transactionOptions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "isolation": { "type": "string", "enum": ["default", "read-uncommitted", "read-committed", "repeatable-read", "serializable"] },
        "readOnly": { "type": "boolean" },
        "timeoutMs": { "type": "integer", "minimum": 100, "maximum": 3600000 }
      },
      "required": ["isolation", "readOnly", "timeoutMs"]
    },
    "transactionHandle": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.-]+$",
      "minLength": 32,
      "maxLength": 2048
    },
    "providerListRequest": {
      "type": "object",
      "additionalProperties": false,
      "maxProperties": 0
    },
    "providerListResult": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "providers": { "type": "array", "maxItems": 256, "items": { "$ref": "#/$defs/providerDescriptor" } } },
      "required": ["providers"]
    },
    "metricsRequest": {
      "type": "object",
      "additionalProperties": false,
      "maxProperties": 0
    },
    "runtimeHealth": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "status": { "type": "string", "enum": ["idle", "ready", "degraded", "unavailable"] },
        "activeGenerations": { "type": "integer", "minimum": 0 },
        "healthyGenerations": { "type": "integer", "minimum": 0 },
        "unhealthyGenerations": { "type": "integer", "minimum": 0 },
        "drainingGenerations": { "type": "integer", "minimum": 0 },
        "closeFailedGenerations": { "type": "integer", "minimum": 0 }
      },
      "required": ["status", "activeGenerations", "healthyGenerations", "unhealthyGenerations", "drainingGenerations", "closeFailedGenerations"]
    },
    "poolMetrics": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "openConnections": { "type": "integer", "minimum": 0 },
        "idleConnections": { "type": "integer", "minimum": 0 },
        "inUseConnections": { "type": "integer", "minimum": 0 },
        "maxOpenConnections": { "type": "integer", "minimum": 0 },
        "waiting": { "type": "integer", "minimum": 0 },
        "inFlight": { "type": "integer", "minimum": 0 },
        "waitCount": { "type": "integer", "minimum": 0 },
        "waitDurationMs": { "type": "integer", "minimum": 0 },
        "nodeReserved": { "type": "integer", "minimum": 0 },
        "budgetRejected": { "type": "integer", "minimum": 0 },
        "acquireSucceeded": { "type": "integer", "minimum": 0 },
        "acquireTimeouts": { "type": "integer", "minimum": 0 },
        "queueRejected": { "type": "integer", "minimum": 0 },
        "forcedDrains": { "type": "integer", "minimum": 0 },
        "closeFailures": { "type": "integer", "minimum": 0 }
      },
      "required": ["openConnections", "idleConnections", "inUseConnections", "maxOpenConnections", "waiting", "inFlight", "waitCount", "waitDurationMs", "nodeReserved", "budgetRejected", "acquireSucceeded", "acquireTimeouts", "queueRejected", "forcedDrains", "closeFailures"]
    },
    "transactionMetrics": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "active": { "type": "integer", "minimum": 0 },
        "capacity": { "type": "integer", "minimum": 1 },
        "begins": { "type": "integer", "minimum": 0 },
        "commits": { "type": "integer", "minimum": 0 },
        "rollbacks": { "type": "integer", "minimum": 0 },
        "expired": { "type": "integer", "minimum": 0 },
        "lost": { "type": "integer", "minimum": 0 },
        "rejected": { "type": "integer", "minimum": 0 }
      },
      "required": ["active", "capacity", "begins", "commits", "rollbacks", "expired", "lost", "rejected"]
    },
    "metricSample": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z][a-z0-9_]{2,159}$" },
        "kind": { "type": "string", "enum": ["counter", "gauge"] },
        "unit": { "type": "string", "enum": ["connections", "operations", "transactions", "milliseconds", "generations", "bytes"] },
        "value": { "type": "integer", "minimum": 0 },
        "labels": {
          "type": "object",
          "maxProperties": 4,
          "properties": { "provider": { "$ref": "#/$defs/providerId" } },
          "additionalProperties": false
        }
      },
      "required": ["name", "kind", "unit", "value"]
    },
    "runtimeMetricsResult": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "schemaVersion": { "type": "integer", "const": 1 },
        "observedAt": { "type": "string", "format": "date-time" },
        "health": { "$ref": "#/$defs/runtimeHealth" },
        "pools": { "$ref": "#/$defs/poolMetrics" },
        "transactions": { "$ref": "#/$defs/transactionMetrics" },
        "samples": { "type": "array", "maxItems": 256, "items": { "$ref": "#/$defs/metricSample" } }
      },
      "required": ["schemaVersion", "observedAt", "health", "pools", "transactions", "samples"]
    },
    "probeRequest": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "connection": { "$ref": "#/$defs/connectionSpec" } },
      "required": ["connection"]
    },
    "probeResult": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "ready": { "type": "boolean" },
        "providerId": { "$ref": "#/$defs/providerId" },
        "latencyMs": { "type": "integer", "minimum": 0 },
        "message": { "type": "string", "maxLength": 1024 }
      },
      "required": ["ready", "providerId", "latencyMs"]
    },
    "activateRequest": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "connection": { "$ref": "#/$defs/connectionSpec" } },
      "required": ["connection"]
    },
    "activateResult": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "connection": { "$ref": "#/$defs/connectionRef" },
        "generation": { "type": "integer", "minimum": 1 },
        "ready": { "type": "boolean" }
      },
      "required": ["connection", "generation", "ready"]
    },
    "retireRequest": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "connection": { "$ref": "#/$defs/connectionRef" } },
      "required": ["connection"]
    },
    "queryRequest": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "connection": { "$ref": "#/$defs/connectionRef" },
        "statement": { "$ref": "#/$defs/statement" },
        "transactionHandle": { "$ref": "#/$defs/transactionHandle" },
        "maxRows": { "type": "integer", "minimum": 1, "maximum": 10000 }
      },
      "required": ["connection", "statement", "maxRows"]
    },
    "executeRequest": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "connection": { "$ref": "#/$defs/connectionRef" },
        "statement": { "$ref": "#/$defs/statement" },
        "transactionHandle": { "$ref": "#/$defs/transactionHandle" }
      },
      "required": ["connection", "statement"]
    },
    "column": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": { "type": "string", "minLength": 1, "maxLength": 256 },
        "databaseType": { "type": "string", "maxLength": 128 },
        "nullable": { "type": "boolean" }
      },
      "required": ["name", "databaseType", "nullable"]
    },
    "queryResult": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "columns": { "type": "array", "maxItems": 1024, "items": { "$ref": "#/$defs/column" } },
        "rows": {
          "type": "array",
          "maxItems": 10000,
          "items": { "type": "array", "maxItems": 1024, "items": { "$ref": "#/$defs/value" } }
        },
        "truncated": { "type": "boolean" }
      },
      "required": ["columns", "rows", "truncated"]
    },
    "executeResult": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "rowsAffected": { "type": "integer", "minimum": 0 } },
      "required": ["rowsAffected"]
    },
    "beginRequest": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "connection": { "$ref": "#/$defs/connectionRef" },
        "options": { "$ref": "#/$defs/transactionOptions" }
      },
      "required": ["connection", "options"]
    },
    "beginResult": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "transactionHandle": { "$ref": "#/$defs/transactionHandle" },
        "expiresAt": { "type": "string", "format": "date-time" }
      },
      "required": ["transactionHandle", "expiresAt"]
    },
    "endTransactionRequest": {
      "type": "object",
      "additionalProperties": false,
      "properties": { "transactionHandle": { "$ref": "#/$defs/transactionHandle" } },
      "required": ["transactionHandle"]
    }
  }
}
