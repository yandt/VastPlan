# VastPlan

基于 LLM 的通用 Agent 系统，面向企业级客户：在线 Agent 开发 + 远程客户端本地执行脚本/工作流 + **全栈插件市场**（类似 VSCode 的在线插件机制）。

## 文档

所有项目文档从 **[docs/dev/00-index.md](docs/dev/00-index.md)** 进入——它是文档的唯一索引。开发前先查阅相关设计篇。

## 文档维护纪律（务必遵守）

1. **单一真相源**：同一件事只有一篇权威文档，发现重复立即合并。
2. **代码与文档同一次改动**：改模块就在同一次提交里改它的设计文档。
3. **决策进 ADR**：任何架构取舍写成 `docs/dev/decisions/` 下的 ADR，只追加不覆盖。
4. **过期即删**：宁可删掉过期文档，也不留误导内容。

## 已确定的方向

- 插件运行模型：**全栈插件**（前端 UI + 后端逻辑 + 客户端脚本），见 ADR-0001。
- 技术栈：**全新选型**，不沿用 testa，见 ADR-0002。
- 参考系统：`../testa`（方向一致，可借鉴设计经验，不直接复用代码）。

## 插件开发语言选型

- 规划任何新插件或插件重大改造时，必须先进行开发语言选型，不默认沿用现有语言。**Go 原生、Python、Node.js** 是重点比较基线，但不是限制名单；只要插件 Runtime 和平台契约能够支持，也可以选择 Rust、Java、C# 等更适合该场景的语言。
- 选型首先看运行效率、资源占用、生态与驱动成熟度以及业务场景适配度，再综合比较并发模型、热更新需求、进程隔离与安全边界、跨平台部署、可维护性和团队工程成本。
- 规划方案必须明确给出候选语言的优劣、推荐语言及理由；不得为了语言统一牺牲性能、生态或场景适配度。必要时允许通过稳定 RPC 契约连接多语言 Provider。
- 运行方式与开发语言分别决策：同时判断插件应内嵌、进入该语言的共享 Runtime 进程，还是使用独立隔离进程。

## 模型选择协作规则

- 默认按任务复杂度选择模型：内核架构、安全边界、集群与运行时、跨模块重构、复杂故障分析等高风险或需要全局推理的任务，推荐 **5.6-sol**；边界明确的插件实现、前端组件、测试、文档和常规修复，推荐 **5.6-terra**。
- 每次任务开始先完成这一判断。若推荐模型与当前主对话模型不一致，必须先明确提示用户手工切换模型，并在用户确认已切换前不开展实质性实现工作。
- 不得以启动子代理或后台任务的方式绕过上述手工切换确认；无法可靠判断时，优先建议使用 **5.6-sol**。

## 代码分解与分层规则

- 禁止为了交付速度持续扩大单一大文件。新增职责前先判断是否属于独立领域、工作流、端口或适配器；属于则先拆分再实现。
- 前端必须分离语义/状态逻辑与 UI 渲染：业务插件使用框架无关契约和 Workbench 定义，Engine/Renderer/Shell/Workbench 各自分包；网络、缓存、Generation、Worker、身份和视图组件不得堆入同一入口文件。
- 后端按“入口适配器 → 应用工作流/用例 → 领域规则 → 端口 → 基础设施适配器”组织。HTTP/NATS/gRPC handler 不得直接承载完整业务事务，工作流不得反向依赖具体传输或存储实现。
- 拆分以清晰依赖方向和单一职责为目标，不按行数机械切文件；共享类型放在中立契约包，禁止通过循环 import 或通用 `utils` 大包掩盖边界不清。
- 修改已有大文件时，不继续内联新职责；优先把本次触及的职责抽成可独立测试的模块，并在同次改动中补齐测试和设计文档。

## 多实现协议注入规则

- 存在开发/生产、Provider、存储、认证、传输、Runtime 或其他多实现路线时，必须在组合根选择一次具体协议实现，并以接口或策略对象向后注入。
- 后续各层只能调用统一协议，不得继续传递模式字符串、布尔开关或环境标记，再由每一层分别编写条件分支。
- 环境差异、候选解析、校验规则、缓存策略和失败语义应由同一协议实现集中拥有；工作流、Loader 与基础设施适配器只依赖协议接口。
- 新增实现时优先增加协议实现并复用既有调用链，不得复制一条仅适用于该实现的平行流程。测试必须证明首次执行、嵌套调用、热替换和恢复路径遵循同一协议。

## Workbench 数据驱动规则

- 所有功能页面必须通过 Workbench 契约声明页面、字段、列、动作、表单与 Overlay；不得向组合组件传入业务 React 节点、框架组件、任意样式或逐按钮回调。
- 每个 `ActionSpec` 必须显式声明稳定 ID、本地化标题、语义图标和 placement。图标只能来自 UI Contract 词表；缺少准确图标时先扩充统一图标库及全部 Renderer 映射，禁止根据动作 ID 猜测或使用默认业务图标。
- 表单与 Overlay 动作只引用受治理的定义 ID；其他动作统一由页面级 `runAction(context, signal)` 工作流端口按 action ID 分派。Workbench 只负责布局、状态、Tooltip、确认流程和回调上下文，不承载业务分支。
- TypeScript 类型、SDK 定义入口与 Portal Runtime 必须同时校验上述规则，确保编译期、第一方注册和绕过 Helper 的运行时注册使用同一协议。

## UI 契约兼容性同步规则

- 修改公共 UI Contract、Workbench 定义、Renderer/Shell/Workbench 导出形状或其 major/minor 兼容边界时，必须在同一次改动中同步 canonical SDK 版本、全部受影响插件 Manifest 与模块导出、Portal Platform Catalog/Profile 精确引用、插件 SemVer、测试夹具和设计文档；不得只修改调用端或单个 Renderer。
- 开发态 HMR 必须把基础 UI 模块图与 RuntimeSpec 的 `uiContract` 作为同一 Generation 原子切换；同一契约族出现不同范围时必须拒绝提交，不能生成“新模块 + 旧 Profile”的混合运行描述。
- 契约修改完成后必须运行仓库 UI 契约同步门禁、完整前端测试与 Portal 构建；本地已存在 Activation 时，还必须执行 `./engineering/tools/platform-dev.sh bootstrap --rebuild-seed` 并读取 `/__vastplan_dev/runtime` 验证 Render Adapter、Shell、Workbench 与实际模块导出兼容后，才可宣告完成。

## 插件边界与物理拆分规则

- 新增插件前必须先写清楚它拥有的真实边界：独立替换/版本、在线配置、状态或凭证、外部系统适配、故障隔离、集群生命周期、按需下载中至少一项。全部不具备时，默认不得创建独立插件。
- 逻辑贡献不等于物理进程。多个高频、无状态、同信任域的规则应优先作为同一 Provider 内的独立模块或签名数据 Bundle；不得为了“一条贡献一个插件”增加进程、制品和组合配置。
- 禁止“元数据空壳插件”：插件必须真正拥有清单宣称的运行生命周期和替换边界。实现仍硬编码在内核时，只能保留中立契约或内部适配缝，不能伪装成已完成的可替换插件。
- 同一业务能力的 Backend/Frontend/Desktop/Mobile 贡献默认交付为一个全栈插件；只有存在独立发布、替换、权限或故障边界时才拆成多个制品。代码仍必须在插件内部按端和工作流分包。
- SDK 只放协议客户端、生命周期适配和安全默认值；不得放具体 Provider 或业务实现。无权限、确定性的纯计算放 Library；需要状态、凭证、网络、集群或隔离的实现才进入插件。
- 测试、演示和 Runtime 验证插件必须位于 `engineering/e2e/fixtures/plugins` 或 `examples/plugins`，使用 test/example 命名空间，不得混入生产 `cn.vastplan.foundation.*` 目录和平台可配置目录。
- 前端 Catalog、Renderer、Shell Library 等可保持独立签名与按需下载，但在产品分类上属于 Foundation Library/基础模块，不作为普通应用插件暴露给服务配置。
- 如果通用 Resolver、启动器或内核为了某一个具体插件家族新增特例，应立即触发边界复审：优先修正制品类型或组合模型，不以更多特例维持过细拆分。
