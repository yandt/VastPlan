# Standard Shell Composition

本基础插件是 Portal 的 Slot 单一管理层。它固定页面、导航语义区和 `shell.*` / `page.*` Slot 名称，执行作用域、排序和冲突收敛；不包含 CSS，也不决定菜单位于顶部还是侧栏。

功能插件只能注册页面并向这些稳定 Slot 填充组件。布局插件只消费本插件生成的组合模型，因此更换布局不会改变功能插件契约。
