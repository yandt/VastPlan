{
  "id": "cn.vastplan.test.runtime.python-subinterpreter-hello",
  "name": "Python Subinterpreter Hello",
  "version": "0.3.1",
  "publisher": "vastplan",
  "license": "Apache-2.0",
  "licenseFile": "LICENSE",
  "noticeFile": "NOTICE",
  "description": "仅用于 E2E 的 CPython 子解释器 Runtime Host 与纯 Python 桥接夹具",
  "engines": { "backend": "^0.1 || ^1.0" },
  "capabilities": { "kernelServices": ["kernel.info"], "credentials": [], "resources": [] },
  "contextAccess": { "required": ["scope.tenant"], "optional": ["trace", "request.deadline"] },
  "execution": {
    "backend": {
      "driver": "python-subinterpreter",
      "minimumIsolation": "trusted-runtime",
      "requirements": { "python": ">=3.14" },
      "python": { "subinterpreterSafe": true }
    }
  },
  "runtime": {
    "instancePolicy": "active-active", "stateModel": "external-shared", "visibility": "cluster", "routing": "queue",
    "provides": [{ "extensionPoint": "tool.package", "capability": "test.runtime.python-subinterpreter-hello", "contractVersion": "1.0.0", "visibility": "cluster", "routing": "queue" }]
  },
  "activation": ["onAgentTool:test.runtime.python-subinterpreter-hello"],
  "entry": { "backend": "backend/main.py" },
  "contributes": {
    "backend": {
      "tools": [{
        "id": "test.runtime.python-subinterpreter-hello",
        "service_role": "backend",
        "title": "Python Subinterpreter Hello",
        "subcommands": [{
          "name": "echo",
          "description": "由 CPython 子解释器回显输入",
          "paramsSchema": {
            "type": "object",
            "properties": { "text": { "type": "string" } },
            "required": ["text"]
          }
        }, {
          "name": "whoami",
          "description": "通过子解释器 HostCall 桥接读取内核信息"
        }]
      }]
    }
  }
}
