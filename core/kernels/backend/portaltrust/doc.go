// Package portaltrust owns the Backend trusted boundary that verifies signed
// frontend artifacts, materializes immutable browser/server module graphs and
// exposes only narrow Composer kernel services. It is not an HTTP server and
// never handles browser sessions.
package portaltrust
